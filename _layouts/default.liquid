<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{ site.title }}</title>
    <meta name="description" content="{{ site.description }}">
    <meta property="og:title" content="{{ site.title }}">
    <meta property="og:description" content="{{ site.description }}">
    <meta property="og:image" content="{{ site.base_url }}/preview.png">
    <meta property="og:url" content="{{ site.base_url }}">
    <meta property="og:type" content="website">
    <meta name="twitter:card" content="summary_large_image">
    <link rel="stylesheet" href="{{ site.base_url }}/style.css">
    <link rel="icon" href="{{ site.base_url }}/favicon.ico" type="image/x-icon">
  </head>
  <body>
    {{ page.content }}
  </body>
</html>
